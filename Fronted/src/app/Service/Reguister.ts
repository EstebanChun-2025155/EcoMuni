// Compatibilidad con el nombre anterior: utiliza el servicio HTTP de autenticación.
export { AuthService } from './auth.service';
export type { DatosRegistro as RegisterData } from './auth.service';
